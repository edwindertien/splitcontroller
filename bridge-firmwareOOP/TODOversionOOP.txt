NOTE:
build in Linux CLI (because the Arduino IDE is kinda buggy)   :
arduino-cli compile --fqbn arduino:avr:diecimila bridge-firmwareOOP &>> debuglog.txt

TODO:

readInput in operate();

W

Rename elements of m_PWMpins:
joy (nunchuck?) xyz



int directionA  in case GREEN bij LeftMode, does A mean 'left'?

void init() {
  Wire.setClock(10000) ;
  pinMode(20, INPUT_PULLUP);
  pinMode(21, INPUT_PULLUP);

  what are pin # 20 and 21 for?

switch leftmode, rightmode

Rezip, compile and install NunchuckFunctions library
